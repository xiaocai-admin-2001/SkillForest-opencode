# Invalid configuration - security issues

[SERVICE]
    Flush        5
    Log_Level    info

[INPUT]
    Name    tail
    Path    /var/log/*.log
    Tag     app.*

[OUTPUT]
    Name            es
    Match           *
    Host            elasticsearch.default.svc
    Port            9200
    HTTP_User       admin
    HTTP_Passwd     hardcoded-password-123
    tls             Off
    Retry_Limit     5