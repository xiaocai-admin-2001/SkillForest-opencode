# Invalid Fluent Bit configuration - OpenTelemetry issues

[SERVICE]
    Flush        5
    Log_Level    info

[INPUT]
    Name    tail
    Path    /var/log/*.log
    Tag     app.*

[OUTPUT]
    Name            opentelemetry
    Match           *
    # Missing Host parameter (required)
    Port            99999
    Header          Authorization Bearer hardcoded-token-123
    tls             Off
    tls.verify      Off