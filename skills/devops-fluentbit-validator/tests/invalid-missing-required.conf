# Invalid configuration - missing required parameters

[SERVICE]
    Flush        5
    Log_Level    info

[INPUT]
    # Missing Name parameter
    Tag    test.*

[FILTER]
    # Missing Name and Match parameters
    Kube_URL   https://kubernetes.default.svc:443

[OUTPUT]
    # Missing Name and Match parameters
    Host    elasticsearch.default.svc