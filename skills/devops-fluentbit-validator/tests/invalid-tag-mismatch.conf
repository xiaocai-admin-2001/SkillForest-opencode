# Invalid configuration - tag mismatches

[SERVICE]
    Flush        5
    Log_Level    info

[INPUT]
    Name    tail
    Path    /var/log/app.log
    Tag     app.logs

[FILTER]
    Name     grep
    Match    database.*
    Regex    level ERROR

[OUTPUT]
    Name         stdout
    Match        nonexistent.*
    Format       json
    Retry_Limit  3